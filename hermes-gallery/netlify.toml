[build]
  command = "npm install && sed -i \"s|__SUPABASE_URL__|$SUPABASE_URL|g; s|__ANON_KEY__|$ANON_KEY|g\" index.html"
  publish = "."

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[build.environment]
  NODE_VERSION = "20"